/* Enum of game states */
export const GameState = Object.freeze({
  LOBBY: 'lobby',
  STARTING: 'starting',
  PLAYING: 'playing',
  ENDING: 'ending',
  RESULTS: 'results',
})

/* Enum of routes */
export const RoutePath = Object.freeze({
  HOME: 'Home Page',
  GAME: 'Game Page',
  MATCH_HISTORY: 'Match History Page',
})
